pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        mavenLocal()
    }
}
rootProject.name = "SqlCipherKotlinMP"

include(":KmpSqlencrypt")